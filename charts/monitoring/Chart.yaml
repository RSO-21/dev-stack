apiVersion: v2
name: monitoring
description: Wrapper chart to deploy Kubernetes monitoring (Prometheus + Grafana) via kube-prometheus-stack
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: kube-prometheus-stack
    version: ">=57.0.0 <60.0.0"
    repository: https://prometheus-community.github.io/helm-charts
